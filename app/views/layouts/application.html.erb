<!DOCTYPE html>
<html>
<head>
  <title>FoodApi</title>
  <%= csrf_meta_tags %>
  <style>@import 'https://fonts.googleapis.com/css?family=Roboto:300,400,700';</style>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
  <div class="container">
    <div class="header">
      <%= render "/partials/navigation" %>
      <% if not notice.nil? %>
      <div class="notice"><i class="material-icons dp48" style="color: #4F8A10;">done</i><%= notice %></div>
      <% end %>
      <% if not alert.nil? %>
      <div class="alert"><i class="material-icons dp48">report_problem</i><%= alert %></div>
      <% end %>
    </div>
    <div class="body">
      <%= yield %>
    </div>
    <div class="footer top-space">
      <div class="footer-container">
        <p class="text-muted">Desarrollado por el Centro Nacional de Investigación y Desarrolo Tecnológico</p>
      </div>
      </div>
  </div>
</body>
</html>
