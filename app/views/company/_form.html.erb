<h2>Actualizar datos de compañia</h2>
<%= form_for(@company) do |f|  %>
<% if flash[:error]%>
<div class="alert">
	<i class="material-icons dp48">report_problem</i>
	<strong>Error al realizar la operación:</strong>
	<ul type = "circle">
		<% flash[:error].each do |message| %>
			<li><i class="material-icons dp48">error</i><%= message %></li>
		<%end%>
	</ul>
</div>
<% end %>
<div class="input-field">
	<%= f.label :nombre %>
	<%= f.text_field :nombre, :required => true %>
</div>
<div class="input-field">
	<%= f.label :direccion, "Dirección" %>
	<%= f.text_field :direccion, :required => true %>
</div>
<div class="input-field">
	<%= f.label :telefono, "Telefóno" %>
	<%= f.text_field :telefono, :required => true %>
</div>
<div class="action top-space center">
	<%= f.submit "Enviar", class:"btn" %>
</div>
<%end%>
